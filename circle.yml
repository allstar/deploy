machine:
  python:
    version: 2.7.5
dependencies:
  override:
    - pip install ansible
compile:
  override:
    - /usr/bin/true
test:
  override:
    - /usr/bin/true
deployment:
  prod:
    branch: master
    commands: ./run.sh
