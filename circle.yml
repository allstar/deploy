machine:
  python:
    version: 2.7.5
test:
  override:
    - "true"
deployment:
  prod:
    branch: master
    commands:
      - kubectl replace --force -f walletdb.yml
      - kubectl replace --force -f statedb.yml
      - "curl -f -u $CIRCLE_TOKEN: -X POST https://circleci.com/api/v1.1/project/github/phoebus-games/smoke/tree/master"
