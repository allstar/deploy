---
apiVersion: v1
kind: Service
metadata:
  name: gf
  labels:
    app: gf
spec:
  ports:
  - port: 8080
    name: gf
  selector:
    app: gf
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: gf-deployment
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: gf
    spec:
      containers:
      - name: gf
        image: alexec/gf:latest
        imagePullPolicy: Always
        env:
          - name: JAVA_OPTS
            value: -Xmx256m
          - name: MONGODB_HOST
            value: statedb
        ports:
        - containerPort: 8080
        readinessProbe:
          httpGet:
            path: /games/roulette
            port: 8080
            httpHeaders:
            - name: PlayerId
              value: "0"
            - name: Wallet
              value: http://wallet:8080
          initialDelaySeconds: 15
          periodSeconds: 15
          timeoutSeconds: 10
        livenessProbe:
          httpGet:
            path: /games/roulette
            port: 8080
            httpHeaders:
            - name: PlayerId
              value: "0"
            - name: Wallet
              value: http://wallet:8080
          initialDelaySeconds: 15
          periodSeconds: 15
          timeoutSeconds: 10
